datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model Hint {
  hintId Int @id @default(autoincrement())
  questionId String
  content String
}