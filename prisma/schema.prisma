datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id String @id
  name String
  email String
  password String
}

model Score {
  scoreId Int @id @default(autoincrement())
  userId String 
  score Int
}